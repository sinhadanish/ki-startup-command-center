# =============================================================================
# MODEL CONTEXT PROTOCOL (MCP) SERVERS CONFIGURATION
# =============================================================================
# This file contains environment variables and configuration settings for
# all MCP servers configured for the Ki Startup Command Center project.
# 
# INSTRUCTIONS:
# 1. Replace all placeholder values with your actual credentials
# 2. Keep this file secure and never commit it to version control
# 3. Some servers may require additional setup or authentication steps
#
# Last updated: 2025-05-31
# =============================================================================


# =============================================================================
# DATABASE SERVERS
# =============================================================================

# PostgreSQL Configuration
# Used by: postgres MCP server
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_USER=postgres
POSTGRES_PASSWORD=password
POSTGRES_DB=ki_db
DATABASE_URL=postgresql://postgres:password@localhost:5432/ki_db

# Redis Configuration
# Used by: redis MCP server
REDIS_URL=redis://localhost:6379
REDIS_PASSWORD=
REDIS_PORT=6379


# =============================================================================
# AI & MACHINE LEARNING
# =============================================================================

# OpenAI Configuration
# Used by: analytics MCP server
OPENAI_API_KEY=sk-your-openai-api-key-here

# Anthropic Configuration
# Used by: analytics MCP server
ANTHROPIC_API_KEY=sk-ant-your-anthropic-api-key-here


# =============================================================================
# COLLABORATION & COMMUNICATION
# =============================================================================

# GitHub Configuration
# Used by: github MCP server
GITHUB_TOKEN=your-github-personal-access-token
GITHUB_USERNAME=your-github-username
GITHUB_REPOSITORY=your-repository-name

# Slack Configuration
# Used by: slack MCP server
SLACK_BOT_TOKEN=xoxb-your-slack-bot-token
SLACK_WEBHOOK_URL=https://hooks.slack.com/services/your/slack/webhook
SLACK_SIGNING_SECRET=your-slack-signing-secret

# Google Drive Configuration
# Used by: gdrive MCP server
GDRIVE_CLIENT_ID=your-google-client-id
GDRIVE_CLIENT_SECRET=your-google-client-secret
GDRIVE_REFRESH_TOKEN=your-google-refresh-token
GDRIVE_FOLDER_ID=your-gdrive-folder-id


# =============================================================================
# CALENDAR & SCHEDULING
# =============================================================================

# Calendar Configuration
# Used by: calendar MCP server
CALENDAR_CLIENT_ID=your-google-client-id
CALENDAR_CLIENT_SECRET=your-google-client-secret
CALENDAR_REFRESH_TOKEN=your-google-refresh-token
CALENDLY_ACCESS_TOKEN=your-calendly-access-token


# =============================================================================
# AUTOMATION & WORKFLOWS
# =============================================================================

# n8n Configuration
# Used by: n8n MCP server
N8N_BASIC_AUTH_USER=admin
N8N_BASIC_AUTH_PASSWORD=ki2024
N8N_WEBHOOK_URL=http://localhost:5678/
N8N_ENCRYPTION_KEY=your-encryption-key


# =============================================================================
# WEB AUTOMATION
# =============================================================================

# Puppeteer Configuration
# Used by: puppeteer MCP server
PUPPETEER_HEADLESS=true
PUPPETEER_SLOWMO=0
PUPPETEER_EXECUTABLE_PATH=
PUPPETEER_USER_DATA_DIR=
PUPPETEER_DEFAULT_VIEWPORT_WIDTH=1280
PUPPETEER_DEFAULT_VIEWPORT_HEIGHT=800


# =============================================================================
# FILESYSTEM & STORAGE
# =============================================================================

# Filesystem Configuration
# Used by: filesystem MCP server
FILESYSTEM_ROOT_DIR=/Users/snapsprint/Documents/Ki Master Folder
FILESYSTEM_ALLOWED_EXTENSIONS=.txt,.md,.js,.ts,.py,.html,.css,.json,.yml,.yaml
FILESYSTEM_MAX_FILE_SIZE=10485760

# AWS S3 Configuration (for file storage)
# Used by: filesystem MCP server (optional)
AWS_ACCESS_KEY_ID=your-aws-access-key
AWS_SECRET_ACCESS_KEY=your-aws-secret-key
AWS_REGION=us-east-1
AWS_S3_BUCKET=ki-platform-storage


# =============================================================================
# ANALYTICS & MONITORING
# =============================================================================

# Analytics Configuration
# Used by: analytics MCP server
POSTHOG_API_KEY=your-posthog-api-key
POSTHOG_HOST=https://app.posthog.com
SENTRY_DSN=your-sentry-dsn


# =============================================================================
# DOCKER & CONTAINER MANAGEMENT
# =============================================================================

# Docker Configuration
# Used by: docker MCP server
DOCKER_HOST=unix:///var/run/docker.sock
DOCKER_COMPOSE_FILE=/Users/snapsprint/Documents/Ki Master Folder/startup-command-center/docker-compose.yml


# =============================================================================
# DOCUMENTATION & KNOWLEDGE MANAGEMENT
# =============================================================================

# Documentation Configuration
# Used by: docs MCP server
DOCS_ROOT_DIR=/Users/snapsprint/Documents/Ki Master Folder/startup-command-center/docs
DOCS_FORMAT=markdown
DOCS_AUTO_INDEX=true


# =============================================================================
# ENVIRONMENT MANAGEMENT
# =============================================================================

# Environment Configuration
# Used by: env MCP server
ENV_FILE_PATH=/Users/snapsprint/Documents/Ki Master Folder/startup-command-center/.env
ENV_BACKUP_DIR=/Users/snapsprint/Documents/Ki Master Folder/startup-command-center/.env-backups

